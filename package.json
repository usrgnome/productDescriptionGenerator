{
  "name": "description-migrator",
  "version": "1.0.0",
  "description": "Rewrite descriptions based on product image, features",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.json",
    "start": "tsc -p tsconfig.json && node ./build/index.js",
    "updater": "tsc -p tsconfig.json && node ./build/mongoUpdater.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^3.0.0",
    "csv-writer": "^1.6.0",
    "dotenv": "^16.4.5",
    "image-to-base64": "^2.2.0",
    "mongodb": "^6.6.2",
    "openai": "^4.47.1",
    "ts-node": "^10.9.2"
  }
}
